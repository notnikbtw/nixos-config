# Nix build results
result
result-*

# direnv
.direnv
.envrc

# Editor/OS junk
*.swp
*~
.DS_Store

# systemd coredumps if any end up in the repo dir
core.*

# Machine-specific - regenerate with `nixos-generate-config` on each machine
hardware-configuration.nix
